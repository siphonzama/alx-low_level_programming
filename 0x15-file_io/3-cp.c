#include "main.h"

/**
 * new_file-give 1024 bytes for a buffer
 * @file: the name of the file buffer is stores char for
 *
 * Return: a pointer to the new
 */

char *new_file(char *file);
void close_file(int nf);
char *new_file(char *file);

{
	char *file;

	file = mall
